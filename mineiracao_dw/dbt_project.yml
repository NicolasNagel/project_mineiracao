name: 'mineiracao_dw'
version: '1.0.0'

profile: 'mineiracao_dw'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

vars:
    "dbt_date:time_zone": "Amercia/Sao_Paulo"

clean-targets:         
  - "target"
  - "dbt_packages"

models:
  mineiracao_dw:
    staging:
      +materialized: view

    intermediate:
      +materialized: table
      
    mart:
      +materialized: table
